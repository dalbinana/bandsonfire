<div class="jumbotron">
  <% if user_signed_in? %>
    <h1>Welcome back, <%= current_user.name %></h1>
    <p class="lead">
      Are your bands hot right now? Check it!
    </p>
    <%= link_to "See bands", user_bands_path(current_user), :class => 'btn btn-large btn-danger' %>
  <% else %>
    <h1>Welcome, new user!</h1>
    <p class="lead">
      You must sign in or sign up before you can access our super sweet,
      top secret content.
    </p>
    <div class="row-fluid">
      <div class="span6">
        <%= link_to "Sign In", new_user_session_path, :class => "btn btn-success" %>
      </div>
      <div class="span6">
        <%= link_to "Sign Up", new_user_registration_path, :class => "btn btn-success" %>
      </div>
    </div>
  <% end %>
</div>

    <div class="table-responsive">
                <table class="table table-striped spoticontent">
                
                    <tr>
                    <th class="col-md-3">Image</th>
                    <th class="col-md-6">Name of the band</th> 
                    <th class="col-md-2">Popularity</th>
                    <th class="col-md-1">Songs</th>
                    </tr>
                    <% @bands_to_show.each do |band| %>


                    <tr><td><img class='picture_bands' style='background-image: url("<%= band[:picture] %>")'</td><td><%= band[:name] %> </td> <td><%= band[:popularity] %> </td></tr>
                    <% end %>

                                

                </table>
            </div>  

